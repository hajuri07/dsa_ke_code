#include <iostream>
using namespace std;
int main(){
    int arr[10] = {1,2,3,4,5};
    int n =5,pos=2,x=67;
    for(int i =n; i>pos;i--){
        arr[i]=arr[i-1];
        
        
    }
    arr[pos]=x;
    n++;
    
    for(int i =0;i<n;i++){
        cout<<arr[i]<<" ";
        
    }
